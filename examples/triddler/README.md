Triangular ("triddler") puzzles, for testing the triangular code paths.

`blob.g` is in the Olsak format, generated from a hexagon of side 4. Unlike `examples/png/`,
these are clue-only files, so loading one exercises solving as well as parsing.
